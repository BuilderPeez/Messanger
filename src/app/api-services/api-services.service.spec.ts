import { ApiServicesService } from './api-services.service';

describe('ApiServicesService', () => {
  const service: ApiServicesService = new ApiServicesService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
