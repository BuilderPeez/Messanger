import { PagesModule } from './pages.module';

describe('PagesModule', () => {
  const module: PagesModule = new PagesModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
